import { Typography } from 'antd';

const { Title } = Typography;

export default function CreationStudioPage() {
  return (
    <div>
      <Title level={3}>创作工作室</Title>
      <p>一键成片、视频预览、任务进度、导出功能将在此实现。</p>
    </div>
  );
}
