import { Typography } from 'antd';

const { Title } = Typography;

export default function MaterialManagementPage() {
  return (
    <div>
      <Title level={3}>素材管理</Title>
      <p>素材上传、列表、AI 打标、向量检索功能将在此实现。</p>
    </div>
  );
}
