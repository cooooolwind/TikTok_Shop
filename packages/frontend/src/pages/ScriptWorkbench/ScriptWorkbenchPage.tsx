import { Typography } from 'antd';

const { Title } = Typography;

export default function ScriptWorkbenchPage() {
  return (
    <div>
      <Title level={3}>剧本工作台</Title>
      <p>剧本生成、分镜编辑、爆款仿写、模板管理功能将在此实现。</p>
    </div>
  );
}
