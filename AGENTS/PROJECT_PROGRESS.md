# 模块进度总览（按阶段排列）

> 基于 `PROJECT_PLAN.md` 的模块 A–H 及其子任务，按 Phase 1–4 排列，标注功能清单与实际实现进度。
>
> 状态图例：✅ 已完成 · 🔨 部分完成 · ⬜ 未开始

---

## Phase 1：工程就绪 + 核心链路打通（Week 1）

> **目标**：仓库可运行、前后端骨架就位、跑通最简一键成片

### 模块 A：工程基础（P0）

| #  | 任务                                                          | 状态 | 说明                                                                                               |
| -- | ------------------------------------------------------------- | :--: | -------------------------------------------------------------------------------------------------- |
| A1 | Monorepo 初始化（pnpm workspaces + Turborepo + shared-types） |  ✅  | pnpm-workspace.yaml、turbo.json、shared-types 包均已配置并导出真实类型                             |
| A2 | ESLint + Prettier + Husky + lint-staged + commitlint          |  ✅  | 全部配置就位；Husky 仅有内部脚本，无自定义 hook 文件                                               |
| A3 | 后端骨架：NestJS + TypeORM + Swagger + 健康检查               |  ✅  | main.ts 配置完成，包含全部模块注册及 `/api/v1/health`                                            |
| A4 | 前端骨架：React + Vite + Ant Design + Router + 布局           |  ✅  | MainLayout + 侧边栏导航 + 路由懒加载完整                                                           |
| A5 | Docker Compose + Makefile                                     |  ✅  | `docker-compose.dev.yml`、`docker-compose.yml`、`docker-compose.prod.yml` 与 Makefile 皆就绪 |
| A6 | 数据库 Migration 初始化                                       |  ✅  | DatabaseModule + TypeORM 配置就绪；dev环境使用 synchronize: true，prod 关闭                        |
| A7 | 前后端 Mock 约定：OpenAPI spec + Mock Server                  |  ✅  | Swagger `/api/docs`，`MOCK_MODE` 环境变量及 `VolcanoClientProvider` Mock 实现就位            |

### 模块 B（Phase 1 部分）：素材上传

| #  | 任务                            | 状态 | 说明                                                                          |
| -- | ------------------------------- | :--: | ----------------------------------------------------------------------------- |
| B1 | 素材上传 API（类型校验 + 存储） |  ✅  | `POST /materials/upload`，multer 文件校验，本地存储 + DB 写入及查询功能完备 |

### 模块 D（Phase 1 部分）：核心链路打通

| #  | 任务                                         | 状态 | 说明                                                                        |
| -- | -------------------------------------------- | :--: | --------------------------------------------------------------------------- |
| D2 | 任务队列：BullMQ + 进度上报 + Socket.IO 推送 |  ✅  | 4个队列定义，ScriptGenerationProcessor 和 VideoGenerationProcessor 完全实现；视频生成按分镜 independent 并行 pipeline 聚合进度 |
| D3 | 火山引擎视频生成 API 封装                    |  ✅  | ChatCompletion、Seedream 首帧生成与 Seedance 视频生成 API 真实接入（含Mock fallback），TTS仍为 stub        |

---

## Phase 2：核心链路 MVP（Week 2）

> **目标**：跑通完整「素材 → 剧本 → 成片」链路

### 模块 B（续）：素材管理

| #  | 任务                                             | 状态 | 说明                                                                                                                                       |
| -- | ------------------------------------------------ | :--: | ------------------------------------------------------------------------------------------------------------------------------------------ |
| B2 | 素材管理前端（上传、列表、预览、删除、来源声明） |  ✅  | 全量功能实现，包含上传弹窗、各种条件筛选、批删与详情查看                                                                                   |
| B3 | 多模态理解：视觉模型打标（异步任务）             |  ✅  | 实现火山引擎多模态分析，并在分析前自动执行**智能转码与压缩**（强制 yuv420p，码率 >6Mbps 自动压缩），确保浏览器兼容性并优化加载性能；图片/视频分析提示词已集中治理到 `packages/backend/src/ai/prompts`。 |
| B4 | 视频切片：按场景自动切分 + 元数据存储            |  ✅  | 实现基于 AI 语义理解的自动分镜切分，自动保存分镜元数据，并支持从首个分镜自动提取视频封面。                                                 |

### 模块 C：剧本模块（P0）

| #  | 任务                                   | 状态 | 说明                                                                                                                                                          |
| -- | -------------------------------------- | :--: | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1 | 参考视频合入素材系统（结构化拆解） |  ✅  | 参考视频直接通过 `POST /materials/upload`（`source_declaration=reference`）上传并录入，复用多模态大模型异步拆解架构，结合 Hook 提取与 `MaterialAnalysis` 副表实现统一管理，废除了原有的独立表及接口 |
| C2 | 灵感模板 CRUD                          |  ✅  | 完整 CRUD 及测试用例，包含内置模板                                                                                                                            |
| C3 | 剧本生成核心 Prompt 链                 |  ✅  | `ScriptsService.generate` 入队，BullMQ Processor 完整调用多模态模型；剧本 Prompt 已升级为结构化蓝图格式，固定输出“基础设定 / 氛围与画质 / 声音 / 分镜画面内容”，同时强化电商转化导向，要求 3 秒 Hook、卖点分配、商品可见、购买理由和 CTA 收尾；生成入口的整条剧本目标时长支持 4-30 秒，单个分镜仍保持 4-12 秒并在长剧本中拆成多个分镜；素材 AI 分析、视频语义切片和原爆款参考拆解均可进入剧本生成上下文；生成入口支持“自动判断 / 生成台词 / 不生成台词”三档台词策略，后端会按策略同步控制 `dialogue/subtitle`，并在禁用台词时兜底清理声音字段中的旁白、口播和对白文本；剧本、素材、视频和模板提示词已统一收敛到 `packages/backend/src/ai/prompts` 并补充集中 builder 测试 |
| C4 | 分镜脚本结构化输出（JSON Schema）      |  ✅  | AI 返回 `script_blueprint + scenes` 结构化数据，`scripts.script_blueprint` 以 JSONB 保存全局设定与逐分镜镜头语言；当 AI 只返回蓝图分镜时，后端会自动映射为旧 `scenes`，保持视频生成链路兼容                                                                                           |
| C5 | 爆款仿写模式                           |  ✅  | 前端在生成面板提供 `imitation` 入口，可选择已分析完成的参考视频；后端校验参考素材状态并组合原参考视频拆解上下文，引导 AI 在结构化蓝图中进行 1:1 框架仿写生成剧本 |
| C6 | 剧本工作台前端                         |  ✅  | `ScriptWorkbenchPage` 列表及 `ScriptGenerate` 表单，模板管理器均完整；生成表单已在偏好设置中提供台词生成三档选择，且**全量完成移动端卡片式重构**                                                |
| C7 | 剧本干预（分镜增删、台词改写、重生成） |  ✅  | 后端接口完整，前端 `ScriptEditor` 已调整为轻量编辑体验：左侧保留紧凑概要、商品图和原爆款参考摘要，主工作区默认展示“剧本蓝图”，通过“蓝图编辑 / 生成预览”切换查看派生分镜，避免预览内容和技术字段长期铺开；蓝图分镜可直接编辑画面内容、声音、台词/旁白和字幕，预览区展示台词但不展示 `visual_prompt`；支持结构化剧本蓝图编辑保存、旧 scene 高级编辑及WebSocket进度订阅（无拖拽排序）；保存蓝图时会同步更新对应旧 scene 字段，避免蓝图和视频生成字段分裂                                                                   |

### 模块 D（Phase 2 部分）：成片 + 配音 + 预览

| #  | 任务              | 状态 | 说明                                                                                                                                                        |
| -- | ----------------- | :--: | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1 | 一键成片 Pipeline |  ✅  | `GenerationService.create` 到 `VideoGenerationProcessor` 完整 pipeline，按分镜并行调用火山引擎并持久化结果；Seedream 首帧与 Seedance 分镜视频提示词已从 processor 抽取到集中 prompt builder，并会读取 `script_blueprint` 的基础设定、氛围画质、声音规则和当前分镜镜头语言作为强上下文                                                        |
| D4 | TTS 配音对接      |  ⬜  | TTS Module 存在（Service/Controller），但返回空声音列表和 stub preview，无真实 TTS 引擎对接                                                                 |
| D5 | 视频预览 + 导出   |  ✅  | 后端提供导出URL，前端 `VideoPreview` 和 `TaskDetail` 支持分段预览；任务详情页的“预览”主入口已统一跳转到视频剪辑页，任务详情页不再展示删除视频和导出完整视频入口，导出集中在剪辑页完成；字幕首次初始化会优先使用已生成分镜片段的实际时长，并为相邻自动字幕保留 `0.01s` 起始间隔；单段视频预览还会按当前片段时间范围过滤字幕，避免剧本预计时长与模型生成时长不一致、浏览器片段末尾 `timeUpdate` 越界或镜头边界命中导致字幕提前；完整视频仅在点击导出时按需拼接，若任务存在字幕工程文件则自动走 Remotion 字幕烧录导出 |

### 模块 E：用户体验（P0）

| #  | 任务                                     | 状态 | 说明                                                                                                       |
| -- | ---------------------------------------- | :--: | ---------------------------------------------------------------------------------------------------------- |
| E1 | 任务进度展示（阶段状态 + 进度条）        |  ✅  | 后端 `TasksGateway` WebSocket 发送分镜级进度，前端 `useTaskSubscription` hook 渲染分镜步骤与整体进度条 |
| E2 | 失败兜底：错误分类 + 友好提示 + 一键重试 |  ✅  | 视频生成支持错误分类、失败镜头提示、并行生成中已成功分镜保留，并可只补失败镜头                                 |
| E3 | 首页工作台（快速入口 + 最近任务 + 历史） |  ✅  | `HomePage` UI 完整（快速成片入口、4 张概览卡片），通过 `/analytics/home-stats` API 获取真实 DB 统计数据（视频/素材/剧本数量 + 成功率） |

---

## Phase 3：体验打磨 + 进阶功能（Week 3）

> **目标**：补齐 P1 功能，提升产品完整度

### 模块 B（续）：向量检索

| #  | 任务                                       | 状态 | 说明                                                                                                                                                            |
| -- | ------------------------------------------ | :--: | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B5 | pgvector Embedding 生成 + 向量检索 API     |  ✅  | 向量维度改为2048；集成火山多模态 Embedding API；素材分析后自动生成文本向量；searchSimilar 支持语义搜索+文本降级；新增回填端点 POST /materials/search/backfill |
| B6 | 素材检索前端（关键词 + 标签 + 相似度搜索） |  ✅  | 新增 SemanticSearchModal 语义搜索弹窗；MaterialDetail 页面新增「相似素材」推荐区域 |

### 模块 C（续）：剧本自动化

| #  | 任务                                  | 状态 | 说明                                                                                                                                 |
| -- | ------------------------------------- | :--: | ------------------------------------------------------------------------------------------------------------------------------------ |
| C8 | 剧本自动化（策略库 + 因子库动态组合） |  ⬜  | 无动态因子库引擎，`batchGenerate` 返回 stub（`count: 0`）；剧本生成使用单 Prompts 调用火山引擎 AI，模板中有策略/因子字段但未组合 |

### 模块 D（续）：智能剪辑 + 分镜编辑器

| #  | 任务                                       | 状态 | 说明                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| -- | ------------------------------------------ | :--: | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D6 | 智能剪辑：分镜拼接 + 转场 + 字幕（FFmpeg/Remotion） |  🔧  | 已接入分镜优先视频 prompt、结构化剧本蓝图上下文、Seedream 商品图参考首帧生成和按需 FFmpeg 拼接；Seedream 首帧不携带台词文本，Seedance 视频 prompt 保留口播台词作为声音/节奏参考，并显式禁止画面内字幕、标题、水印和乱码文字；相关提示词已集中维护在 `packages/backend/src/ai/prompts/video-generation.prompt.ts`；字幕工程默认优先使用分镜台词，台词为空才回退字幕字段；`packages/video-renderer/` 独立包提供转场与字幕烧录合成，后端 `RemotionRenderingService` 调用 pnpm 子进程渲染含转场和字幕的完整视频；前端 **视频编辑器**（`VideoEditor.tsx`）提供时间轴、拖拽排序、转场预设、字幕 JSON 工程文件加载/编辑、字幕轨和 Remotion 实时预览，初次进入会按分镜顺序自动铺入已生成片段且导出后直接触发浏览器下载；逐分镜先用商品图作为 Seedream 参考生成商品锚定首帧，再将首帧作为 Seedance `first_frame` 并行生成视频片段；FFmpeg 拼接、Remotion 转场渲染和字幕烧录均已实现，但 **TTS 配音叠入、BGM 混音尚未实现** |
| D7 | BGM 配乐库 + 按风格自动匹配                |  ⬜  | BGM module 返回空列表，前端有 UI 但无数据                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| D8 | 分镜级编辑器前端（时间轴 + 拖拽）          |  ✅  | `VideoEditor.tsx` 完整实现：多轨道时间轴、片段拖拽排序、Trim 裁切、转场预设（fade/slide/wipe/zoom_blur）、Remotion 实时预览、Zustand 编辑器 Store；移动端提示使用桌面端编辑                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D9 | 分镜干预 API（单分镜重生成/替换/调时长）   |  🔨  | 剧本级 `regenerateScene` 为同步占位（写入 placeholder 文本），视频级 `regenerateScene` 入队 BullMQ 但无处理器消费 `regenerate-scene` 任务                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

### 模块 E（续）：响应式

| #  | 任务                                  | 状态 | 说明                                                                                      |
| -- | ------------------------------------- | :--: | ----------------------------------------------------------------------------------------- |
| E4 | 响应式布局（桌面优先 + 移动端可查看） |  ✅  | Ant Design 栅格及自定义响应式钩子，全量支持移动端网格导航、卡片式列表及适配后的分镜编辑器 |

### 模块 G：质量保障与运维

| #  | 任务                                            | 状态 | 说明                                                                                                                                     |
| -- | ----------------------------------------------- | :--: | ---------------------------------------------------------------------------------------------------------------------------------------- |
| G1 | 后端核心逻辑单元测试                            |  ✅  | 12 个核心模块及 Service 的 spec 测试文件，覆盖剧本、生成、素材分析、视频拼接、火山客户端等                                               |
| G2 | 前端组件测试                                    |  🔨  | 12 个前端测试文件，覆盖编辑器组件（VideoEditor/Playhead/RemotionPreview）、辅助函数、MaterialCard、TaskCard 和进度；但核心页面测试仍缺失 |
| G3 | E2E 冒烟测试（一键成片链路）                    |  ⬜  | 无 Playwright 或其他 E2E 配置                                                                                                            |
| G4 | 结构化日志（Pino）+ traceId                     |  🔨  | `LoggingInterceptor` 输出带 traceId 的请求日志，但异常过滤器未记录，且未使用 Pino                                                      |
| G5 | CI Pipeline：lint + test + build + Docker image |  ✅  | 完整 CI（turbo --affected）+ CD（ACR 推送 + ECS 部署）                                                                                   |

---

## Phase 4：看板 + 加分项 + 交付（Week 4）

> **目标**：数据看板、创新功能选做、准备提报材料

### 模块 F：数据看板（P1）

| #  | 任务                                          | 状态 | 说明                                                                                             |
| -- | --------------------------------------------- | :--: | ------------------------------------------------------------------------------------------------ |
| F1 | Mock 数据服务与外部真实数据源代理 |  ✅  | `AnalyticsMockGenerator` 基于种子参数的确定性 PRNG 生成自洽 Mock；新增 `MOCK_DASHBOARD` 开关，关闭时自动通过 `MockProxyInterceptor` 将产出外的指标请求透明代理至 `STATISTIC_API_URL` 真实统计中心 |
| F2 | 看板 API + 前端页面（生成量/成功率/耗时）     |  ✅  | 产出总览页：5 张 StatCard + 日历热力图 + 归因柱状图 + 耗时环形图 + 素材类型/品类分布饼图；真实 SQL 聚合 DB 数据                                                                                                                     |
| F3 | 生成因子 × 转化效果归因可视化                 |  ✅  | 侧边栏拆为 4 子页展开组：产出总览 `/analytics/overview`、成本分析（瀑布图+嵌套环形+模板气泡）`/analytics/cost`、转化分析（转化流水卡+桑基图+气泡矩阵）`/analytics/conversion`、策略洞察（Hook 卡片+黄金配方+A/B 雷达图）`/analytics/strategy` |

### 模块 H：创新加分项（P2）

| #  | 任务                                       | 状态 | 说明   |
| -- | ------------------------------------------ | :--: | ------ |
| H1 | A/B 自动出片对比                           |  ⬜  | 无实现 |
| H2 | Agent 编排（LangGraph 多步骤决策）         |  ⬜  | 无实现 |
| H3 | 合规审核流（内容安全 + 版权 + 审核状态机） |  ⬜  | 无实现 |
| H4 | Prompt 市场（分享/收藏优质 Prompt 模板）   |  🔨  | 已实现轻量级模板广场（支持启用模板及填写商品信息生成），但未实现支付、创作者入驻等交易能力 |

---

## 汇总统计

| 阶段           | 模块               |   总任务数   |  ✅ 已完成  | 🔨 部分完成 |  ⬜ 未开始  |
| -------------- | ------------------ | :----------: | :----------: | :----------: | :----------: |
| Phase 1        | A 工程基础         |      7      |      7      |      0      |      0      |
| Phase 1        | B1（素材上传）     |      1      |      1      |      0      |      0      |
| Phase 1        | D2–D3（核心链路） |      2      |      2      |      0      |      0      |
| Phase 2        | B2–B4（素材管理） |      3      |      3      |      0      |      0      |
| Phase 2        | C1–C7（剧本）     |      7      |      7      |      0      |      0      |
| Phase 2        | D1, D4–D5（成片） |      3      |      2      |      0      |      1      |
| Phase 2        | E1–E3（体验）     |      3      |      3      |      0      |      0      |
| Phase 3        | B5–B6（向量检索） |      2      |      2      |      0      |      0      |
| Phase 3        | C8（剧本自动化）   |      1      |      0      |      0      |      1      |
| Phase 3        | D6–D9（智能剪辑） |      4      |      1      |      2      |      1      |
| Phase 3        | E4（响应式）       |      1      |      1      |      0      |      0      |
| Phase 3        | G1–G5（质量）     |      5      |      2      |      2      |      1      |
| Phase 4        | F1–F3（看板）     |      3      |      3      |      0      |      0      |
| Phase 4        | H1–H4（加分项）   |      4      |      0      |      1      |      3      |
| **合计** |                    | **46** | **32** | **5** | **9** |
