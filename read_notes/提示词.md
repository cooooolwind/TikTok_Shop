# 前端开发 AI 指挥官提示词 (电商 AIGC 视频生成系统)

> 作为资深全栈/前端开发专家，请仔细阅读当前项目目录下的 `read_notes`（以及我提供的项目实施计划和 API 接口文档），透彻理解“电商 AIGC 视频生成系统”的前端架构设计和核心业务链路。
> 
> **项目技术栈锁定：**
> 项目技术栈在项目实施计划.md文件里面
> 
> **开发指令与约束：**
> 请**绝对不要**尝试一次性输出所有的前端代码。为了保证代码的高内聚、低耦合以及工程质量，请你严格按照以下步骤与我进行交互式开发。在每一步完成后，必须等待我的确认才能进入下一步：
> 
> **第 1 步：架构与骨架确认**
> 基于需求，规划并输出前端的目录结构（如 `src/pages`, `src/components`, `src/store`, `src/services` 等），并列出完整的路由表设计。要求结构清晰，符合大型前端项目的最佳实践。
> 
> **第 2 步：核心类型与状态定义**
> 根据 API 文档，在 `types` 目录下定义核心数据实体（如 Material, Script, Task 等）的 TypeScript Interface。同时，设计 Zustand 的 Store 结构，不写具体实现，只写出 State 和 Action 的签名。
> 
> **第 3 步：模块化页面开发（按优先级）**
> 按照基础框架搭建 -> 素材管理模块 -> 剧本生成模块 -> 创作工作室的顺序逐个组件编写。每次只输出一个完整的页面或核心组件。
> 
> **第 4 步：复杂交互与联调**
> 针对 WebSocket 任务进度推送、分镜时间轴拖拽等复杂交互，重点实现并优化。
> 
> **代码质量与认知要求：**
> 1. 所有组件必须使用 Functional Component 和 Hooks。
> 2. **拒绝黑盒代码与机械复制**：在编写复杂的业务逻辑（如长轮询兜底、状态流转、复杂组件生命周期）时，必须添加详细的行级注释。你需要向我解释代码底层的运行逻辑和架构考量，确保我能从原理层面完全吃透这些实现机制。
> 3. 严格遵守 ESLint 和 TypeScript 类型校验，禁止使用 `any`，确保类型的绝对安全。
> 
> 准备好了吗？如果理解了以上所有要求，请先执行**第 1 步**，输出前端目录结构与路由规划。